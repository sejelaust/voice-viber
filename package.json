{
  "name": "voice-viber",
  "version": "0.1.0",
  "description": "Local dictation app with Whisper transcription and Ollama cleanup.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/config.js && node --check src/pipeline.js && node --check src/insert.js && node --check src/preload.js && node --check src/recorder-preload.js && node --check src/renderer/app.js && node --check src/renderer/recorder.js"
  },
  "devDependencies": {
    "electron": "^37.0.0"
  }
}
